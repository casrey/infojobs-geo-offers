import { TextInput } from "@tremor/react";
import { SearchIcon } from "@heroicons/react/solid";

const Search = () => {
  return <TextInput icon={SearchIcon} placeholder="Search..." />;
};

export default Search;
